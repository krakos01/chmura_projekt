apiVersion: v1
kind: Service
metadata:
  name: fishing-forum-frontend
  labels:
    app.kubernetes.io/name: fishnet
    app.kubernetes.io/component: frontend
spec:
  selector:
    app: fishing-forum-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
